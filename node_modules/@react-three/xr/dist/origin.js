import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useThree } from '@react-three/fiber';
import { forwardRef } from 'react';
import { xrReferenceSpaceContext } from './contexts.js';
import { useXR } from './xr.js';
/**
 * component for setting the origin of the player (their feet)
 */
export const XROrigin = forwardRef(({ children, ...props }, ref) => {
    const xrCamera = useThree((s) => s.gl.xr.getCamera());
    const referenceSpace = useXR((xr) => xr.originReferenceSpace);
    if (referenceSpace == null) {
        return null;
    }
    return (_jsxs("group", { ref: ref, ...props, children: [_jsx("primitive", { object: xrCamera }), _jsx(xrReferenceSpaceContext.Provider, { value: referenceSpace, children: children })] }));
});
